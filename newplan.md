# `newplan.md` 계획: 이마트 과자 주문을 4번째 브랜드 탭으로 확장

## Summary
- 기존 음료 주문 앱은 그대로 유지하고, 주문 화면에 `EMART`를 `4번째 브랜드 탭`으로 추가한다.
- 현재 탭 순서는 `STARBUCKS`, `TWOSOME`, `EMART`, `COFFEE_BEAN`, `EDIYA`로 정리한다.
- 이마트 상품은 실시간 조회가 아니라 정적 스냅샷 방식으로 관리한다. 대상 대분류는 3개다.
  - `과자/쿠키/파이`
  - `사탕/캬라멜/껌`
  - `초콜릿/초코바`
- 가격은 저장하지 않고 `상품명`, `이미지`, `원본 상품 링크`만 수집한다.
- 하위 카테고리는 EMART 탭 안에서 `대분류 섹션 + 서브카테고리 칩` 구조로 반영한다.

## Key Changes
- 브랜드 구조 확장
  - `Brand`에 `EMART` 추가
  - 로고 탭 순서를 `스타벅스 > 투썸플레이스 > 이마트 > 커피빈 > 이디야`로 고정
  - `STARBUCKS`, `TWOSOME`, `EMART`는 활성, `COFFEE_BEAN`, `EDIYA`는 비활성 유지
- 메뉴 데이터 구조 확장
  - 기존 `Menu` 스키마는 유지하되 `subcategory?: string` 필드를 추가
  - 기존 음료 메뉴는 `subcategory` 없이 그대로 유지
  - EMART 메뉴는 `category`에 대분류, `subcategory`에 하위 카테고리를 저장
  - EMART `availableSizes`는 `["단일"]` 고정
- EMART 대분류/하위 카테고리 정의
  - `과자/쿠키/파이`
    - `일반과자`
    - `쿠키/비스킷`
    - `크래커`
    - `샌드/웨하스`
    - `스틱과자/빼빼로`
    - `파이/케익`
    - `과자선물세트`
    - `유아과자/시리얼`
  - `사탕/캬라멜/껌`
    - `하드캔디`
    - `캬라멜/소프트캔디`
    - `엿/달고나`
    - `껌`
    - `사탕선물세트`
  - `초콜릿/초코바`
    - `초콜릿`
    - `초코바`
    - `초콜릿선물세트`
- EMART 수집 방식
  - 입력은 3개 대분류 URL 고정
  - 각 대분류와 서브카테고리를 순회해 상품 `itemId`, `itemNm`, 이미지 URL, 상세 링크를 추출
  - 메뉴 ID는 `emart-<dispCtgId>-<subCategoryKey>-<itemId>` 형식으로 고정
  - 기존 스타벅스/투썸 메뉴는 보존하고 EMART 항목만 생성 또는 갱신
- 주문 화면 UX
  - EMART는 4번째 브랜드 탭으로 진입
  - EMART 화면에서는 대분류 3개를 기존 카테고리 섹션처럼 표시
  - 각 대분류 상단에 `전체 + 서브카테고리 칩` 제공
  - 서브카테고리 칩 선택 시 해당 그룹만 필터링
  - 검색어는 서브카테고리 필터와 함께 동작
  - EMART 상세 모달은 `단일 옵션 + 수량 + 개인 요청사항`만 사용
  - 스타벅스 전용 추천/인기 메뉴 로직은 EMART에 적용하지 않음
- 관리자/집계
  - 브랜드 필터에 `EMART` 추가
  - 주문 요약과 CSV에서는 EMART 카테고리를 `대분류 / 서브카테고리` 형식으로 표시
  - DB 마이그레이션 없이 처리하기 위해 주문 저장 시 EMART `category`는 합쳐진 문자열로 저장
- 배포 경로 정리
  - 메인 `apps/web`, `apps/server` 기준으로 반영
  - `deploy-upload-render`도 동일한 브랜드/타입/메뉴 구조를 같이 맞춰 배포 드리프트 방지

## Public Interfaces / Types
- `Brand`
  - 기존: `STARBUCKS | TWOSOME`
  - 변경: `STARBUCKS | TWOSOME | EMART`
- `Menu`
  - 기존 필드 유지
  - 신규: `subcategory?: string`
- `CartItem`, `OrderItem`, `SummaryRow`
  - 타입 구조는 유지
  - EMART 주문 저장 시 카테고리 표시는 `대분류 / 서브카테고리` 문자열 사용

## Test Plan
- 타입/빌드
  - 웹/서버 `typecheck` 통과
  - 메인 앱과 `deploy-upload-render` 빌드 가능
- 기존 음료 회귀
  - 스타벅스/투썸 메뉴 조회, 검색, 장바구니, 주문 제출이 기존과 동일
  - 커피빈/이디야는 계속 비활성 상태
- EMART 탐색
  - EMART가 4번째 탭 위치에 노출
  - 대분류 3개 노출
  - 각 대분류에 서브카테고리 칩 노출
  - `전체`와 특정 서브카테고리 필터가 정상 동작
  - 검색과 서브카테고리 필터 동시 동작 확인
- EMART 주문
  - 상품 이미지/상품명 노출
  - 단일 옵션과 수량으로 장바구니 추가 가능
  - 주문 제출 후 관리자 목록과 요약에서 `대분류 / 서브카테고리` 확인
- 관리자 기능
  - 브랜드 `EMART` 필터 조회
  - 상태 변경, 삭제, CSV 출력 정상 동작

## Assumptions
- 이번 확장의 최우선 조건은 기존 음료 기능 무손상이다.
- EMART는 별도 화면이 아니라 기존 주문 화면의 4번째 브랜드 탭으로 통합한다.
- 하위 카테고리 UI는 `대분류 아래 서브카테고리 칩` 방식으로 고정한다.
- 가격, 재고, 할인, 배송 정보는 저장하지 않는다.
