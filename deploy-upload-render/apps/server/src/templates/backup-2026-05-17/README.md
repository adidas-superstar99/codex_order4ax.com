# 2026-05-17 UI Backup

This folder stores the pre-redesign templates that were active before the dark neon UI refresh inspired by the reference image.

Backed up files:

- `shared-styles.html`
- `order.html`
- `admin.html`
- `batch-list.html`

To roll back to the previous version, copy these files back into:

- `deploy-upload-render/apps/server/src/templates/`

Then redeploy the latest commit.
